spring.jpa.show-sql=true

#출력되는 쿼리 로그를 H2에서 MySQL 버전으로 (id bigint generated by default as identity -> id bigint not null auto_increment)
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL57Dialect
#spring.jpa.properties.hibernate.dialect.storage_engine=innodb
#spring.datasource.hikari.jdbc-url=jdbc:h2:mem:testdb;MODE=MYSQL
#spring.datasource.hikari.username=sa
#spring.h2.console.enabled=true

#oauth 설정 호출 , gitignore해야하는 private 데이터들
spring.profiles.include=oauth,gitignore

#세션 저장소를 jdbc로 선택
#spring.session.store-type=jdbc

# 앞단 url
#front.blossomwhale.url=http://front.blossomwhale.shop

# 쿠키 domain 설정
server.servlet.session.cookie.domain=blossomwhale.shop

#H2 file db 온
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.hikari.jdbc-url=jdbc:h2:./testDB;AUTO_SERVER=TRUE
spring.datasource.hikari.username=sa
spring.jpa.hibernate.ddl-auto=create-drop
#H2 in-memory db 온

#MySQL 온
spring.jpa.properties.hibernate.format_sql=true

# 반드시 .gitignore에 추가해야되는 비밀 정보에 관한 설정 추가
